
module.exports = {
  doCommands(commands) {
    let initialPosition = false;

    commands.forEach((command) => {
    //   if (!initialPosition) {
    //     if (command === 'PLACE') {

    //     }
    //   } else {
    //   switch (command) {
    //     case ''
    //   }
    // });
    })
  },
}